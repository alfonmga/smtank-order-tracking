build:
  environment:
    php:
      version: 5.5
  tests:
    override:
      -
        command: phpunit --coverage-clover=my-coverage-file
        coverage:
          file: my-coverage-file
          format: php-clover
checks:
    php:
      code_rating: true
      duplication: true
